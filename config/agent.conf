# Конфигурация веб-агента
uid = AGENT-001
server_url = http://localhost:8080
poll_interval = 10
tasks_dir = ./tasks
results_dir = ./results

# Таймауты (секунды)
connection_timeout = 30
task_timeout = 300